fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
python-multipart==0.0.20
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
httpx==0.28.1
redis==5.2.1
celery==5.4.0
boto3==1.35.90
# Caption rasterisation. Pillow's wheels bundle Raqm (the Dockerfile also
# installs libraqm0), which is what shapes and bidi-orders Arabic correctly —
# without it every caption comes out as disconnected, mirrored letters.
Pillow==12.3.0
# Glyph-coverage checks: a display face that lacks "%" or ":" renders tofu, so
# the caption engine reads each font's cmap before choosing it.
fonttools==4.65.0
# Fallback shaping path for builds where Raqm is absent.
arabic-reshaper==3.0.1
python-bidi==0.6.11
pytest==8.3.4
pytest-asyncio==0.25.2
